package com.example.healthy.common

import com.example.healthy.R

enum class FitButtonEnum(val value: Int) {
    LOSE_WEIGHT(R.string.fitButton_lose_weight_text),
    KEEP_WEIGHT(R.string.fitButton_keep_weight_text),
    GAIN_WEIGHT(R.string.fitButton_gain_weight_text)
}
